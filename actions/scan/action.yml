name: Wrangle Source Scan
description: Scan source code with wrangle security tools

inputs:
  tools:
    description: >
      Space-separated list of tools to run. Suffix with :info for
      informational-only (default policy: :fail). Adapter-pattern tools
      are dispatched to run.sh; action-pattern tools are invoked via
      their uses: steps.
    required: false
    default: "osv zizmor scorecard:info dependency-review wrangle-lint"
  go-cache:
    description: >
      Set to "enabled" to cache the Go module + build caches that
      run.sh's `go install` populates (osv-scanner et al. build
      from source — #343). Default off: the cache is ~1.5–3 GB and is
      stored against the calling repo's Actions cache quota, so it is
      opt-in per-repo rather than imposed on every adopter. Leave off
      (or gate off on release) for scan jobs that gate an attested
      build — the Go build cache trusts compiled output on a hit
      (docs/SLSA_L3_AUDIT.md), so an attested path must build cold.
    required: false
    default: ""
  artifact-name:
    description: >
      Name of the workflow artifact the scan output is uploaded under.
      Scan runs in its own job; a build workflow passes a per-build name
      (e.g. internal-python-scan-python) and its build job downloads that into the
      unified metadata/<type>/<sn>/scan/ tree. The standalone scan workflow
      passes scan. The artifact holds <tool>/output.* plus any
      wrangle_attestation_metadata.json — the scan/ subtree's contents, no
      scan/ prefix.
    required: false
    default: "scan"
  retention-days:
    description: >
      Retention for the scan-results artifact. Empty (default) uses the repo
      default — correct for the standalone scan, whose artifact is the
      consumer-facing deliverable. A build workflow folds this into
      <type>-metadata-<sn> within minutes, so it passes "1" to keep the
      transient off the long-lived artifact list.
    required: false
    default: ""
  checkout:
    description: >
      When "true", check out the calling repo before scanning, so a caller
      whose scan job does nothing else can drop its own checkout step.
      Default "false": the caller is responsible for the checkout (the tools
      scan $GITHUB_WORKSPACE, which must already hold the source).
    required: false
    default: "false"
  ref:
    description: >
      Git ref to check out when checkout is "true". Empty (default) uses the
      triggering SHA. Ignored when checkout is "false".
    required: false
    default: ""
  github-token:
    description: >
      Token forwarded to tools that perform online audits (e.g. zizmor's
      GitHub-API checks). Defaults to the workflow token; pass empty to keep
      those tools offline.
    required: false
    default: ${{ github.token }}

runs:
  using: "composite"
  steps:
    # Checked-out source must exist before any tool runs (they scan
    # $GITHUB_WORKSPACE); opt-in so callers that check out themselves don't
    # double-checkout.
    - name: Check out source
      if: inputs.checkout == 'true'
      uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      with:
        persist-credentials: false
        ref: ${{ inputs.ref }}

    - name: Set up wrangle
      shell: bash
      env:
        WRANGLE_ROOT: ${{ github.action_path }}/../..
      run: "$WRANGLE_ROOT/lib/setup.sh"

    # Stage the tool manifest's go.sum as a cache key under a non-lockfile
    # name: the osv adapter scans $GITHUB_WORKSPACE recursively, so a file
    # named go.sum here would be scanned as the adopter's own and report
    # wrangle's tool deps as their findings.
    - name: Stage Go cache key
      if: inputs.go-cache == 'enabled'
      shell: bash
      env:
        WRANGLE_ROOT: ${{ github.action_path }}/../..
      run: |
        mkdir -p "$GITHUB_WORKSPACE/.wrangle"
        cp "$WRANGLE_ROOT/tools/go.sum" "$GITHUB_WORKSPACE/.wrangle/go-mod-cachekey"

    # go-version-file reads the action's own pinned manifest (an absolute
    # path, outside the workspace — setup-go reads it directly, unlike the
    # globbed cache-dependency-path which must stay inside the workspace).
    - name: Set up Go with module + build cache
      if: inputs.go-cache == 'enabled'
      uses: actions/setup-go@4a3601121dd01d1626a1e23e37211e3254c1c06c # v6.4.0
      with:
        go-version-file: ${{ github.action_path }}/../../tools/go.mod
        cache-dependency-path: .wrangle/go-mod-cachekey

    # Run adapter-pattern tools via the orchestrator (run.sh).
    # run.sh strips :policy suffixes and skips action-pattern tools.
    - name: Run adapter-pattern tools
      shell: bash
      # tools input passed via env var to prevent expression injection.
      # ${{ github.action_path }} is safe to interpolate in run: blocks —
      # it is set by GitHub when fetching the composite action and is not
      # attacker-controllable (unlike inputs.* or github.event.*).
      env:
        WRANGLE_TOOLS: ${{ inputs.tools }}
        WRANGLE_ROOT: ${{ github.action_path }}/../..
        WRANGLE_EXTRA_GITHUB_TOKEN: ${{ inputs.github-token }}
        # run.sh's curated-image VSA gate runs `gh attestation verify` in its own
        # process (not an adapter), so it needs gh's own token; adapters stay
        # isolated (env -i) and read only WRANGLE_EXTRA_GITHUB_TOKEN.
        GH_TOKEN: ${{ inputs.github-token }}
        WRANGLE_INSTALL_TIMEOUT: "900"
      run: |
        # shellcheck source=../../lib/env.sh
        source "$WRANGLE_ROOT/lib/env.sh"
        # $WRANGLE_TOOLS is intentionally unquoted so it word-splits into
        # multiple arguments. This is safe because the orchestrator validates
        # each token against ^[a-z][a-z0-9_-]*$ and runs set -f.
        "$WRANGLE_ROOT/run.sh" \
          -s "$GITHUB_WORKSPACE" \
          -o "$WRANGLE_METADATA_DIR" \
          $WRANGLE_TOOLS

    # Action-pattern tools: invoked via local path. Inside a reusable
    # workflow, ./ resolves to this repo at the called ref (the adopter's
    # pinned tag or the PR branch for local CI).
    # Each tool action handles its own log banner and human-readable
    # output generation (output.md) — see tools/<name>/action.yml.
    # Scorecard only runs on push events — it requires GITHUB_TOKEN scopes
    # that aren't available on PRs and uses Docker, which only mounts
    # $GITHUB_WORKSPACE. Skipping on PRs avoids guaranteed failures.
    # TODO: replace with $/tools/scorecard when $/ syntax ships (#136, #137)
    - name: Scorecard
      if: always() && contains(inputs.tools, 'scorecard') && github.event_name != 'pull_request'
      uses: TomHennen/wrangle/tools/scorecard@24cb9c27b13c93125435cba42b7c9d1b896abf59 # main 2026-06-27

    # Dependency Review is PR-only — the upstream action requires
    # github.event.pull_request.base.sha / head.sha to compute the diff.
    # See tools/dependency-review/SPEC.md.
    # TODO: replace with $/tools/dependency-review when $/ syntax ships (#136, #137)
    # Per-tool options are not plumbed through actions/scan inputs (#221):
    # each tool reads its native config file — dependency-review picks up
    # .github/dependency-review-config.yml when the scanned repo has one.
    # The wrapper's inline defaults (fail-on-severity: high) still apply.
    - name: Dependency Review
      if: always() && contains(inputs.tools, 'dependency-review') && github.event_name == 'pull_request'
      uses: TomHennen/wrangle/tools/dependency-review@24cb9c27b13c93125435cba42b7c9d1b896abf59 # main 2026-06-27

    - name: Generate step summary
      if: always()
      shell: bash
      env:
        WRANGLE_ROOT: ${{ github.action_path }}/../..
      run: |
        # shellcheck source=../../lib/env.sh
        source "$WRANGLE_ROOT/lib/env.sh"
        if [[ -d "$WRANGLE_METADATA_DIR" ]]; then
          "$WRANGLE_ROOT/lib/format_sarif_summary.sh" "$WRANGLE_METADATA_DIR" >> "$GITHUB_STEP_SUMMARY"
        fi

    # Per-finding CI-log lines. MUST run before "Check results" so the
    # finding context appears above the failure line — reordering
    # silently defeats issue #158.
    - name: Log findings
      if: always()
      shell: bash
      env:
        WRANGLE_ROOT: ${{ github.action_path }}/../..
      run: |
        # shellcheck source=../../lib/env.sh
        source "$WRANGLE_ROOT/lib/env.sh"
        "$WRANGLE_ROOT/lib/log_findings.sh" "$WRANGLE_METADATA_DIR"

    # Check results: fail the check if any :fail-policy tool reported
    # findings. Tools with :info policy are noted but don't block.
    - name: Check results
      if: always()
      shell: bash
      env:
        WRANGLE_TOOLS: ${{ inputs.tools }}
        WRANGLE_ROOT: ${{ github.action_path }}/../..
      run: |
        # shellcheck source=../../lib/env.sh
        source "$WRANGLE_ROOT/lib/env.sh"
        # $WRANGLE_TOOLS is intentionally unquoted for word-splitting.
        "$WRANGLE_ROOT/lib/check_results.sh" "$WRANGLE_METADATA_DIR" $WRANGLE_TOOLS

    # SARIF upload: per-tool attribution in the Security tab.
    # continue-on-error is legitimate here — Advanced Security may not
    # be available (private repos, no license).
    #
    # Each upload is gated on its tool actually being in the `tools`
    # input. Without the gate, upload-sarif emits noisy `::error::`
    # annotations on every run that omits the tool because the
    # corresponding metadata/<tool>/output.sarif file was never
    # produced — see issue #189.
    - name: Upload OSV SARIF
      if: always() && contains(inputs.tools, 'osv')
      uses: github/codeql-action/upload-sarif@8aad20d150bbac5944a9f9d289da16a4b0d87c1e # v4.36.2
      continue-on-error: true
      with:
        sarif_file: ${{ github.workspace }}/.wrangle/metadata/osv/output.sarif
        category: wrangle/osv

    - name: Upload wrangle-lint SARIF
      if: always() && contains(inputs.tools, 'wrangle-lint')
      uses: github/codeql-action/upload-sarif@8aad20d150bbac5944a9f9d289da16a4b0d87c1e # v4.36.2
      continue-on-error: true
      with:
        sarif_file: ${{ github.workspace }}/.wrangle/metadata/wrangle-lint/output.sarif
        category: wrangle/wrangle-lint

    - name: Upload Zizmor SARIF
      if: always() && contains(inputs.tools, 'zizmor')
      uses: github/codeql-action/upload-sarif@8aad20d150bbac5944a9f9d289da16a4b0d87c1e # v4.36.2
      continue-on-error: true
      with:
        sarif_file: ${{ github.workspace }}/.wrangle/metadata/zizmor/output.sarif
        category: wrangle/zizmor

    # Scorecard does not upload to the Security tab: its value is the score,
    # carried by the scorecard.dev/result/v0.1 attestation, not SARIF
    # findings (informational-only, cf #203).

    # Skip Code Scanning upload on a dep-review tool error: collect_outputs.sh
    # / mark_error.sh write the error marker exactly when the upstream
    # produced no usable findings, and any SARIF on disk in that case is
    # either absent or an empty fallback. Uploading an empty SARIF would
    # overwrite the previous run's findings in the Security tab with
    # `results: []`, silently scrubbing real findings on a transient API
    # failure. Gating on the marker preserves the prior valid state.
    - name: Upload Dependency Review SARIF
      if: always() && contains(inputs.tools, 'dependency-review') && github.event_name == 'pull_request' && hashFiles('.wrangle/metadata/dependency-review/error') == ''
      uses: github/codeql-action/upload-sarif@8aad20d150bbac5944a9f9d289da16a4b0d87c1e # v4.36.2
      continue-on-error: true
      with:
        sarif_file: ${{ github.workspace }}/.wrangle/metadata/dependency-review/output.sarif
        category: wrangle/dependency-review

    # The scan job uploads its tool output (<tool>/output.* plus any
    # wrangle_attestation_metadata.json the engine later signs) as a
    # workflow artifact. A build workflow's build job downloads it into the
    # unified metadata/<type>/<sn>/scan/ tree and owns the final upload; the
    # standalone scan workflow's artifact-name is the consumer-facing scan.
    - name: Upload scan results artifact
      if: always()
      uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
      continue-on-error: true
      with:
        name: ${{ inputs.artifact-name }}
        path: ${{ github.workspace }}/.wrangle/metadata/
        if-no-files-found: ignore
        retention-days: ${{ inputs.retention-days }}
