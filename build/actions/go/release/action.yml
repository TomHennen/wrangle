name: Wrangle Go Release
description: |
  Invoke goreleaser to build (and on tag pushes, publish) a Go
  project; generate the SBOM via syft; compute artifact hashes for
  SLSA provenance. Sister composite to `build/actions/go/checks` —
  the calling reusable workflow runs checks first (with
  `contents: read`), then this composite (with `contents: write`,
  since goreleaser publishes inline). See build/actions/go/SPEC.md
  "Permissions architecture."

  Goreleaser owns its native publish on tag pushes: it creates the
  GitHub Release, attaches archives + checksums, and runs whatever
  downstream verbs the adopter's .goreleaser.yml configures (Docker
  pushes, Homebrew taps, deb/rpm/snap, announce, etc.). wrangle's
  attest job in the reusable workflow produces the provenance
  shortly after.

inputs:
  path:
    description: "Relative path to the directory containing go.mod and .goreleaser.yml"
    required: false
    default: "."
  go-version:
    description: |
      Go version override. If empty, setup-go reads from go.mod's
      `go` directive.
    required: false
    default: ""
  cache:
    description: |
      Whether the Go module + build caches (actions/setup-go's
      `cache` input) are used. "enabled" (default) restores+saves
      both; "disabled" passes `cache: false`. The reusable workflow
      sets this to "disabled" for release builds — the bytes
      wrangle's attest job signs must derive without consulting any
      shared cross-build cache. See SPEC.md "Cache isolation" for
      the L3 reasoning.
    required: false
    default: "enabled"
  publish:
    description: |
      Whether goreleaser publishes the release inline. The calling
      reusable workflow sets this to the AND of `should-release`
      (from the gate job) and `startsWith(github.ref, 'refs/tags/')`
      — both conditions must hold for a publish to actually be
      possible (gh release requires a tag, and the adopter's
      release-events input controls whether wrangle's pipeline
      treats this event as a release at all).

      true  -> goreleaser runs `release --clean` (full publish,
               creates the GitHub Release, runs Docker pushes,
               Homebrew taps, etc. per the adopter's config).
      false -> goreleaser runs `release --clean --snapshot --skip=publish`
               (builds locally, no upload). PR builds use this so
               the pipeline is exercised without publishing.
    required: false
    default: "false"
  install-zig:
    description: |
      When true, install zig (via mlugg/setup-zig) before goreleaser
      runs. zig acts as a drop-in C/C++ cross-compiler so adopters
      can build cgo binaries for linux/{amd64,arm64} +
      darwin/{amd64,arm64} from the single linux/amd64 GHA runner
      using per-cell `CC=zig cc -target <triple>` templates in
      .goreleaser.yml. Default is false — most projects don't need
      cgo cross-compile, and zig install costs ~20-30s + ~50MB.
      See build/actions/go/README.md "Cross-compiling with cgo."
    required: false
    default: "false"

outputs:
  version:
    description: "The release version (git tag) goreleaser used, or 'snapshot' for non-tag builds"
    value: ${{ steps.metadata.outputs.version }}
  hashes:
    description: "Base64-encoded SHA-256 hashes of built artifacts (SLSA provenance subjects; feeds the VSA matrix)"
    value: ${{ steps.hash.outputs.hashes }}
  shortname:
    description: "Path-derived short name for artifact namespacing (e.g., '.' becomes '_')"
    value: ${{ steps.metadata.outputs.shortname }}
  metadata-dir:
    description: "Workspace-relative path containing the SBOM. Format: metadata/go/<shortname>."
    value: ${{ steps.metadata.outputs.metadata-dir }}

runs:
  using: "composite"
  steps:
    - name: Validate inputs
      shell: bash
      env:
        INPUT_PATH: ${{ inputs.path }}
        INPUT_CACHE: ${{ inputs.cache }}
        INPUT_PUBLISH: ${{ inputs.publish }}
        INPUT_INSTALL_ZIG: ${{ inputs.install-zig }}
      run: |
        set -euo pipefail
        "${{ github.action_path }}/validate_inputs.sh" "$INPUT_PATH" "$INPUT_CACHE" "$INPUT_PUBLISH" "$INPUT_INSTALL_ZIG"

    # cgo cross-compile warning — coarse grep over .goreleaser.yml.
    # Adopters with CGO_ENABLED=1 + a multi-arch / cross-OS matrix who
    # haven't opted into zig will hit opaque `# runtime/cgo` assembler
    # errors during goreleaser's per-cell build. cgo_warning.sh surfaces
    # the cause and names the input that fixes it. Suppressed when
    # install-zig is already true.
    - name: Cgo cross-compile warning
      if: inputs.install-zig != 'true'
      shell: bash
      env:
        INPUT_PATH: ${{ inputs.path }}
      run: |
        set -euo pipefail
        "${{ github.action_path }}/cgo_warning.sh" "$INPUT_PATH"

    # mlugg/setup-zig verifies the zig tarball against ziglang.org's
    # published minisign signature (master public key
    # RWSGOq2NVecA2UPNdBUZykf1CCb147pkmdtYxgb3Ti+JO/wCYvhbAb/U). That's
    # a stronger integrity tier than a hardcoded SHA-256 we'd own in
    # tools/zig/install.sh — per CLAUDE.md "Install method and
    # verification", we prefer the canonical-package-manager
    # equivalent (a wrapper action) when it provides better
    # verification. Pinned by SHA per third-party-action rule.
    - name: Install zig (cgo cross-compile toolchain)
      if: inputs.install-zig == 'true'
      uses: mlugg/setup-zig@d1434d08867e3ee9daa34448df10607b98908d29 # v2.2.1
      with:
        version: "0.16.0"

    - name: Compute metadata
      id: metadata
      shell: bash
      env:
        INPUT_PATH: ${{ inputs.path }}
      run: |
        set -euo pipefail
        "${{ github.action_path }}/../compute_metadata.sh" "$INPUT_PATH"

    - name: Setup Go
      uses: actions/setup-go@4a3601121dd01d1626a1e23e37211e3254c1c06c # v6.4.0
      with:
        go-version: ${{ inputs.go-version || '' }}
        go-version-file: ${{ inputs.go-version == '' && format('{0}/go.mod', inputs.path) || '' }}
        cache: ${{ inputs.cache != 'disabled' }}
        cache-dependency-path: ${{ inputs.path }}/go.sum

    # goreleaser-action is a `uses:` step; can't be wrapped inline
    # by stop_commands_guard.sh `run` mode. Use the begin/end form:
    # `begin` emits ::stop-commands::<token> + writes the token to
    # $GITHUB_OUTPUT; `end` (later step, `if: always()`) re-enables.
    # Same pattern container uses for docker/build-push-action.
    - name: Suspend workflow commands (begin)
      id: guard
      shell: bash
      run: |
        set -euo pipefail
        "${{ github.action_path }}/../../../../lib/stop_commands_guard.sh" begin

    - name: Run goreleaser
      uses: goreleaser/goreleaser-action@5daf1e915a5f0af01ddbcd89a43b8061ff4f1a89 # v7.2.2
      with:
        # Pinned literal version (not "~> v2") to match wrangle's
        # supply-chain discipline. See CLAUDE.md "No auto-merge of
        # dependency updates." Dependabot tracks the action SHA; the
        # binary version below is bumped by hand on the 7-day delay.
        version: "2.15.4"
        workdir: ${{ inputs.path }}
        # publish == 'true' → full release (native goreleaser publish).
        # publish != 'true' → snapshot + skip-publish (build only).
        # Caller sets publish from `should-release && startsWith(refs/tags/)`,
        # so the false branch covers both PR builds and main-pushes
        # under tag-only release-events.
        args: ${{ inputs.publish == 'true' && 'release --clean' || 'release --clean --snapshot --skip=publish' }}
      env:
        # goreleaser's publish path needs GITHUB_TOKEN for the GitHub
        # Release create + upload. The caller of the reusable workflow
        # grants contents: write at startup. Harmless when not
        # publishing.
        GITHUB_TOKEN: ${{ github.token }}
        # Pin goreleaser to the tag that triggered this run. When several
        # tags point at the same commit, goreleaser's own resolution picks
        # an arbitrary one and re-targets that tag's existing Release,
        # failing the upload with 422 already_exists. Empty on non-tag
        # (snapshot) builds, which goreleaser treats as unset.
        GORELEASER_CURRENT_TAG: ${{ startsWith(github.ref, 'refs/tags/') && github.ref_name || '' }}

    - name: Resume workflow commands (end)
      if: always()
      shell: bash
      env:
        STOP_COMMANDS_TOKEN: ${{ steps.guard.outputs.stop-commands-token }}
      run: |
        set -euo pipefail
        "${{ github.action_path }}/../../../../lib/stop_commands_guard.sh" end "$STOP_COMMANDS_TOKEN"

    - name: Compute artifact hashes
      id: hash
      shell: bash
      env:
        INPUT_PATH: ${{ inputs.path }}
      run: |
        set -euo pipefail
        "${{ github.action_path }}/compute_hashes.sh" "$INPUT_PATH/dist/checksums.txt"

    - name: Install Cosign (for syft signature verification)
      uses: sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6 # v4.1.2
      with:
        # Must match the cosign built from tools/go.mod — the version the suite tests.
        cosign-release: v3.0.6

    - name: Install syft
      shell: bash
      env:
        WRANGLE_ROOT: ${{ github.action_path }}/../../../..
      run: |
        set -euo pipefail
        "$WRANGLE_ROOT/tools/syft/install.sh"

    - name: Generate SBOM
      shell: bash
      env:
        INPUT_PATH: ${{ inputs.path }}
        METADATA_DIR: ${{ steps.metadata.outputs.metadata-dir }}
        WRANGLE_ROOT: ${{ github.action_path }}/../../../..
      run: |
        set -euo pipefail
        mkdir -p "$METADATA_DIR"
        "$WRANGLE_ROOT/tools/syft/generate_sbom.sh" \
            "$INPUT_PATH" "$METADATA_DIR/sbom.spdx.json"
        "$WRANGLE_ROOT/lib/write_attest_manifest.sh" \
            "$METADATA_DIR" "https://spdx.dev/Document" "sbom.spdx.json"

    - name: Generate summary
      if: always()
      shell: bash
      env:
        INPUT_PATH: ${{ inputs.path }}
        VERSION: ${{ steps.metadata.outputs.version }}
        PUBLISH: ${{ inputs.publish }}
      run: |
        set -euo pipefail
        "${{ github.action_path }}/generate_summary.sh" "$INPUT_PATH" "$VERSION" "$PUBLISH"
