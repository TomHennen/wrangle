on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "**" ]
  workflow_dispatch:

jobs:
  # Whenever new source is pushed or a PR is received, scan it for any issues
  check-change:
    permissions:
      actions: read
      contents: read
      packages: read
      security-events: write # So we can upload sarif
    uses: tomhennen/wrangle/.github/workflows/scan-source.yml@main
