name: Check Change

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["**"]
  workflow_dispatch:

jobs:
  check-change:
    permissions:
      actions: read
      contents: read
      security-events: write
    uses: tomhennen/wrangle/.github/workflows/check_source_change.yml@main
    # No secrets needed — new pipeline uses public binary downloads
