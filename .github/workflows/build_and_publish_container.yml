name: "Build and publish a container using best practices."

on:
  workflow_call:
    inputs:
      path:
        description: "A path, within the repo, to the folder containiner a Dockerfile you want to build."
        required: true
        type: string
      imagename:
        description: "The full path, including registry, specifying where the image should be published."
        required: true
        type: string
      registry:
        required: true
        type: string
      publish_provenance_for_private_repo:
        description: "Publish provenance to Sigstore for a private repo"
        required: false
        default: false
        type: boolean
      release-events:
        description: |
          Which events should trigger SLSA provenance generation and
          verification. One of:
            - non-pull-request (default): every event except pull_request*.
            - tag-only: push to refs/tags/* only.
            - main-and-tags: push to refs/heads/main or refs/tags/*.
            - <comma-separated event_name list>: matches if github.event_name
              is in the list. See actions/release_gate for full vocabulary.
          Note: this gate currently controls provenance generation and
          verification, not the docker push (which happens mid-composite
          in build/actions/container and is gated by the workflow's own
          trigger conditions). See docs/SPEC.md for the asymmetry rationale.
        required: false
        type: string
        default: non-pull-request
      verify-image:
        description: |
          When true (default), wrangle runs `cosign verify-attestation`
          against the just-pushed image's SLSA provenance attestation
          before declaring the workflow successful. Verify failure fails
          the workflow. Set to false to skip wrangle's verification.
          (When the composite action gains cosign image signing, this
          input will also gate `cosign verify` of the signature.)
        required: false
        type: boolean
        default: true
    secrets:
      gh_token:
        description: "GitHub token with write access"
        required: true
    outputs:
      digest:
        description: "Image digest pushed to the registry."
        value: ${{ jobs.build.outputs.digest }}
      imagename:
        description: "Normalized (lowercased) image name."
        value: ${{ jobs.build.outputs.imagename }}
      metadata-artifact-name:
        description: "Name of the workflow artifact (a zip of `metadata/container/<shortname>/`'s contents) containing the SBOM and any scan output. Format: `container-metadata-<shortname>`. Adopters fetch via `actions/download-artifact` — the metadata files land at the top level of the configured `path:`, not under `metadata/container/<shortname>/`."
        value: ${{ jobs.build.outputs.metadata-artifact-name }}
      should-release:
        description: |
          "true" if the current event matches release-events; "false" otherwise.
          Wrangle gates the provenance job on this internally; callers can
          gate downstream release-time jobs on it too.
        value: ${{ jobs.gate.outputs.should-release }}

jobs:
  gate:
    runs-on: ubuntu-latest
    permissions: {}    # release_gate reads only env; no workspace, no API
    outputs:
      should-release: ${{ steps.gate.outputs.should-release }}
    steps:
      # No checkout: release_gate resolves via ${{ github.action_path }}.
      # TODO: replace with $/actions/release_gate when GitHub ships $/ syntax (#136)
      - uses: TomHennen/wrangle/actions/release_gate@0fa729d1b2f2bf5f550af91ed9e06a0d98ab5de9 # claude/implement-npm-build-type-draft 2026-05-04
        id: gate
        with:
          events: ${{ inputs.release-events }}

  build:
    permissions:
      contents: read
      packages: write
    outputs:
      digest: ${{ steps.build.outputs.digest }}
      imagename: ${{ steps.build.outputs.imagename }}
      metadata-artifact-name: container-metadata-${{ steps.build.outputs.shortname }}
    runs-on: ubuntu-latest
    steps:
    # TODO: replace with $/build/actions/container when GitHub ships $/ syntax (#136)
    - name: "build and publish"
      id: build
      uses: TomHennen/wrangle/build/actions/container@0fa729d1b2f2bf5f550af91ed9e06a0d98ab5de9 # claude/implement-npm-build-type-draft 2026-05-04
      with:
        path: ${{ inputs.path }}
        imagename: ${{ inputs.imagename }}
        registry: ${{ inputs.registry }}
        github_token: ${{ secrets.GITHUB_TOKEN }}

  provenance:
    # Gated on release-events (default: non-pull-request) so adopters can
    # tighten when provenance is produced without forking the workflow.
    if: ${{ needs.gate.outputs.should-release == 'true' }}
    needs: [gate, build]
    permissions:
      actions: read # for detecting the Github Actions environment.
      id-token: write # for creating OIDC tokens for signing.
      packages: write # for uploading attestations.
    # MUST use tag, not SHA — the SLSA generator validates its own invocation
    # ref and rejects bare SHAs. GitHub's OIDC token only reports the tag/branch
    # ref, so slsa-verifier cannot verify provenance from SHA-pinned calls.
    # See: https://github.com/slsa-framework/slsa-verifier/issues/12
    uses: slsa-framework/slsa-github-generator/.github/workflows/generator_container_slsa3.yml@v2.1.0 # zizmor: ignore[unpinned-uses] tag required by SLSA generator's OIDC verification
    with:
      image: ${{ needs.build.outputs.imagename }}
      digest: ${{ needs.build.outputs.digest }}
      registry-username: ${{ github.actor }}
      private-repository: ${{ inputs.publish_provenance_for_private_repo }}
    secrets:
      registry-password: ${{ secrets.gh_token }}

  # Verify the SLSA provenance attestation on the just-pushed image.
  # Catches the "registry served bytes that don't match what wrangle
  # pushed" attack window. Failing here fails the workflow, blocking
  # any downstream release-time job via standard `needs:` propagation.
  #
  # Today the verify only covers the SLSA attestation. When the
  # composite action gains `cosign sign` of the image digest, this job
  # will also verify the image signature with the adopter's
  # workflow_ref as the cert identity.
  #
  # The cert identity below is fixed by the SLSA generator's @v2.1.0
  # pin in the provenance: job above — both refs MUST move together.
  # Tests assert the lockstep so a single-side bump fails CI loudly.
  verify:
    if: ${{ needs.gate.outputs.should-release == 'true' && inputs.verify-image }}
    needs: [gate, build, provenance]
    runs-on: ubuntu-latest
    # No registry credentials needed: cosign verify-attestation against
    # ghcr.io public images works anonymously, and the SLSA attestation
    # signature is verified against Sigstore's transparency log (Rekor)
    # which is also public. Adopters with private images that require
    # auth-gated pulls should set `verify-image: false` and verify in
    # their own job — see README.
    permissions: {}
    steps:
      - uses: sigstore/cosign-installer@cad07c2e89fa2edd6e2d7bab4c1aa38e53f76003 # v4.1.1

      - name: Verify SLSA provenance attestation
        env:
          IMAGE_REF: ${{ needs.build.outputs.imagename }}@${{ needs.build.outputs.digest }}
          EXPECTED_REPO: ${{ github.repository }}
        run: |
          set -euo pipefail
          cosign verify-attestation \
            --type slsaprovenance \
            --certificate-identity "https://github.com/slsa-framework/slsa-github-generator/.github/workflows/generator_container_slsa3.yml@refs/tags/v2.1.0" \
            --certificate-oidc-issuer "https://token.actions.githubusercontent.com" \
            --certificate-github-workflow-repository "$EXPECTED_REPO" \
            "$IMAGE_REF" > /dev/null
          printf 'Verified SLSA provenance attestation for %s\n' "$IMAGE_REF"
