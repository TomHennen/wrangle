#!/usr/bin/env bats

# Fixture bats file for wrangle-doc-lint tests — never executed, only
# string-matched for @test names by lint.py.

@test "demo: does the thing" {
    true
}

@test "demo: handles colons: like real wrangle test names do (exit 0)" {
    true
}
